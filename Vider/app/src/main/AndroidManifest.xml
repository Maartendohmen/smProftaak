<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.fhict.fontys.vider">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/googlehomecolor"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/googlehomecolor"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DocterChatActivity" />
        <activity android:name=".DocterListActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeDocterActivity" />
        <activity android:name=".HomePatientActivity"></activity>
    </application>

</manifest>